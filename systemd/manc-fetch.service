# The inputs only (calendar, news, closes), inside the container: no model needed.
[Unit]
Description=manc fetch (podman compose)
After=manc-api.service

[Service]
Type=oneshot
WorkingDirectory=%h/quant/manc
ExecStart=/usr/bin/podman compose run --rm api manc fetch
